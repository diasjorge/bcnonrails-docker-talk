Talk for the barcelona on rails user group about docker and rails.

Includes tkn and copycopter with modifications embedded

To run:

```
   cd tkn
   bundle install
   bundle exec bin/tkn ../slides.rb
```
